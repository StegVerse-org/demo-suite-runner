# StegVerse Demo Suite Runner Dependencies
# Core (no external deps for deterministic layer)

# LLM Adapter (install as needed)
# openai>=1.0.0
# anthropic>=0.20.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Utilities
python-dotenv>=1.0.0
